<%= image_tag(@user.avatar_url) %>
<h1><%= @user.name %></h1>
<br>
<h3>All repositories: </h3>
<%= link_to "View all repositories", user_repositories_path(@user) %>

<br>
<h3>Starred repositories:</h3>
<%= link_to "View starred repositories", user_repositories_path(@user) %>
<br>

<h3>Your followers (<%= @user.followers.count %>):</h3>
<% @user.followers.each do |follower| %>
  <ul>
    <li> <%= follower["login"] %> </li>
  </ul>
  <% end %>
<br>

<h3>Recent activity</h3>
<%= link_to "View user activity", user_activities_path(@user) %>


<h3>You're following (<%= @user.following.count %>):</h3>
<% @user.following.each do |following| %>
  <ul>
    <li> <%= following["login"] %> </li>
  </ul>
  <% end %>
<br>
<h3>Organizations</h3>
<%= link_to "View all organizations", user_organizations_path(@user) %>
